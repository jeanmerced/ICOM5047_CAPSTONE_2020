@model IEnumerable<ICTS_API.Models.Product>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpirationDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VirtualSiteID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiscrepancyExists)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpirationDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VirtualSiteID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiscrepancyExists)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LotID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LotID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LotID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
