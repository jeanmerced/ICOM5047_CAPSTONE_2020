@model IEnumerable<ICTS_API.Models.Cart>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TagAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastUpdated)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NearExpirationDateWarningCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpiredWarningCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiscrepancyExists)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TagAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdated)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NearExpirationDateWarningCount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpiredWarningCount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiscrepancyExists)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CartID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CartID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CartID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
